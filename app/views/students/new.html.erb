<h1>Create a Student</h1>
<h3>Student Form: </h3>

<%= form_for(@student) do |form| %>
	<p><%= form.label :first_name %>
	<%= form.text_field :first_name %></p>
	<p><%= form.label :last_name %>
		<%= form.text_field :last_name %></p>
	<p><%= form.submit "Create Student" %></p>
<% end %>
